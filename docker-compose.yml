version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: arhci-frontend:latest
    container_name: arhci-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
